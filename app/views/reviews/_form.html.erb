<%= simple_form_for([ booking, review ]) do |form| %>
  <%= form.input :content %>
  <%= form.input :stars,
                 collection: (1..5).to_a,
                 label: "Rate your doc",
                 required: true
  %>
  <%= form.button :submit %>
<% end %>

